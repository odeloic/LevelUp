{
  "name": "levelup",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:seed": "node --env-file=.env --import=tsx/esm scripts/seed.ts",
    "db:migrate": "node --env-file=.env --import=tsx/esm scripts/add-xp-events.ts",
    "db:introspect": "drizzle-kit introspect"
  },
  "dependencies": {
    "@libsql/client": "^0.17.2",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "drizzle-orm": "^0.45.2",
    "nuxt": "^4.4.2",
    "vue": "^3.5.31",
    "vue-router": "^5.0.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
